# MaruxOS System Defaults
# ==================================

# Root Password (for initial setup)
ROOT_PASSWORD_HASH="marux"   # 2026-08-27 공개 릴리즈용 기본값(이전 값은 개발자 개인 비번이라 제거)

# Hostname
DEFAULT_HOSTNAME="maruxos"

# Timezone
DEFAULT_TIMEZONE="Asia/Seoul"

# Locale
DEFAULT_LOCALE="ko_KR.UTF-8"
FALLBACK_LOCALE="en_US.UTF-8"

# Keyboard Layout
DEFAULT_KEYBOARD="kr"

# Network
ENABLE_DHCP="yes"
ENABLE_NETWORKMANAGER="yes"

# scripts/archive — superseded build scripts (frozen, unmodified)

Moved here on 2026-08-28 for readability of `scripts/`. Nothing is deleted; each file is exactly as it was when it produced its image. Version-by-version changes: `ISO-BUILD-HISTORY.md`. Current entry points: `scripts/README.md`.
